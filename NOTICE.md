# Notice

This toolkit **wraps** (does not include) the exporter
[BlueMatthew/WechatExporter](https://github.com/BlueMatthew/WechatExporter),
which is licensed **GPL v2**. Its binary is **not** committed here — the setup
step downloads it from that project's Releases. Invoking it from a script is
aggregation, not a derivative work, so the wrapper scripts here are MIT.
The HTML→text converter reads exported files only and is fully independent.
